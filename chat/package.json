{
  "name": "opaque-ts-demo",
  "version": "1.0.0",
  "private": true,
  "description": "Pure TypeScript OPAQUE demo with Chrome extension test harness",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "cd server && npm run dev",
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  }
}
