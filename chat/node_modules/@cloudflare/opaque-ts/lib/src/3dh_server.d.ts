import { AuthFinish, CredentialResponse, ExpectedAuthResult, KE1, KE2 } from './messages.js';
import { Config } from './config.js';
export declare class AKE3DHServer {
    private readonly config;
    constructor(config: Config);
    response(server_private_key: Uint8Array, server_identity: Uint8Array, ke1: KE1, credential_response: CredentialResponse, context: Uint8Array, client_public_key: Uint8Array, client_identity?: Uint8Array): Promise<{
        ke2: KE2;
        expected: ExpectedAuthResult;
    }>;
    finish(auth_finish: AuthFinish, expected: ExpectedAuthResult): {
        session_key: number[];
    } | Error;
}
//# sourceMappingURL=3dh_server.d.ts.map