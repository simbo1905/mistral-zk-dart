import { AKEKeyPair, MemoryHardFn } from './thecrypto.js';
import { CredentialRequest, CredentialResponse, RegistrationRecord, RegistrationRequest, RegistrationResponse } from './messages.js';
import { Config } from './config.js';
export declare class OpaqueCoreClient {
    readonly config: Config;
    private memHard;
    constructor(config: Config, memHard?: MemoryHardFn);
    createRegistrationRequest(password: Uint8Array): Promise<{
        request: RegistrationRequest;
        blind: Uint8Array;
    }>;
    finalizeRequest(password: Uint8Array, blind: Uint8Array, response: RegistrationResponse, server_identity?: Uint8Array, client_identity?: Uint8Array): Promise<{
        record: RegistrationRecord;
        export_key: number[];
    }>;
    createCredentialRequest(password: Uint8Array): Promise<{
        request: CredentialRequest;
        blind: Uint8Array;
    }>;
    recoverCredentials(password: Uint8Array, blind: Uint8Array, response: CredentialResponse, server_identity?: Uint8Array, client_identity?: Uint8Array): Promise<{
        client_ake_keypair: AKEKeyPair;
        server_public_key: Uint8Array;
        export_key: Uint8Array;
    } | Error>;
}
//# sourceMappingURL=core_client.d.ts.map