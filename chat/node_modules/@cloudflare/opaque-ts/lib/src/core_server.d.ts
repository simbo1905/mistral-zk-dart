import { CredentialRequest, CredentialResponse, RegistrationRecord, RegistrationRequest, RegistrationResponse } from './messages.js';
import { Config } from './config.js';
export declare class OpaqueCoreServer {
    readonly config: Config;
    oprf_seed: Uint8Array;
    constructor(config: Config, oprf_seed: Uint8Array);
    private doOPRFEvaluation;
    createRegistrationResponse(request: RegistrationRequest, server_public_key: Uint8Array, credential_identifier: Uint8Array): Promise<RegistrationResponse>;
    createCredentialResponse(request: CredentialRequest, record: RegistrationRecord, server_public_key: Uint8Array, credential_identifier: Uint8Array): Promise<CredentialResponse>;
}
//# sourceMappingURL=core_server.d.ts.map